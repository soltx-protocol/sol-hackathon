import React from 'react';
import { hot } from 'react-hot-loader/root';

import styles from './index.css';

const Account = () => {
	return <div className={styles.account}>123</div>;
};

export default hot(Account);
